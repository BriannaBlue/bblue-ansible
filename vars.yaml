#this is where our ansile variables are going to go so we can SSH into the hosts in our inventory
---
ansible_port: 22 #SSH port
ansible_user: ec2-user #EC2 username 
ansible_ssh_private_key_file: ./ansible_key.pem #Path to my private key

